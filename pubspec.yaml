name: flutter_trivia_clean_arch
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Service locator
  get_it: ^7.1.3
  # Bloc for state management
  flutter_bloc: ^7.0.1
  # Value equality
  equatable: ^2.0.3
  # Functional programming thingies
  dartz: ^0.10.0-nullsafety.2
  # Remote API
  internet_connection_checker: ^0.0.1+1
  http: ^0.13.3
  # Local cache
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.1.4
  bloc_test: ^8.0.2

flutter:
  uses-material-design: true
